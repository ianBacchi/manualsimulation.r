# Função para ler instruções de um arquivo CSV
read_instructions_from_csv <- function(file_path) {
  # Lê o arquivo CSV
  instrucoes <- read.csv(file_path, header = FALSE)
  
  # Converte as instruções em um vetor
  instrucoes <- as.character(instrucoes$V1)
  
  return(instrucoes)
}
